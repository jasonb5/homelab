- hosts: localhost
  gather_facts: false
  roles:
    - vault/client-key-signing
- hosts: all
  roles:
    - bootstrap
- hosts: kubernetes
  tasks:
  - ansible.builtin.apt:
      pkg:
      - nfs-client
      state: present
  tags:
  - packages
