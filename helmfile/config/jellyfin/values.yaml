nodeSelector:
  area: rack

hostNetwork: true

resources:
  limits:
    cpu: 4
    memory: 4Gi

networkPolicy:
  egress:
    - {}

ingress:
  default:
    enabled: true
    host: jellyfin.angrydonkey.io

persistence:
  config:
    enabled: true
    size: 64Gi

  data:
    enabled: true
    type: nfs
    server: 192.168.55.20
    path: /mnt/user/media
